import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pwintern.angular',
  appName: 'pwfrontintern',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
